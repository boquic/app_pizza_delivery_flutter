import 'package:flutter/material.dart';

/// Paleta de colores de Pizzas Reyna
/// Colores estrictos según especificación del diseño
class AppColors {
  AppColors._();

  // Colores principales
  static const Color primary = Color(0xFFF28C0F);
  static const Color secondaryLight = Color(0xFFF2AA6B);
  static const Color accent = Color(0xFFBF3415);
  static const Color error = Color(0xFFF20505);

  // Colores de fondo
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textDisabled = Color(0xFFBDBDBD);

  // Colores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Colores de overlay
  static const Color overlay = Color(0x1F000000);
  static const Color divider = Color(0x1F000000);
}
